var board;
var player;
var checker;

// function setup() {
	// noCanvas();
	// createCanvas(400, 400);
	// background(0);
	// rectMode(CENTER);
	// imageMode(CENTER);
	// board = new Board();

var board = new Board($('body'));
board.show();
	// constructor(w, h, r, c, d, l) {
	// player = new Player();
	// console.log(player);
	// checker = new Checker();
	// board.draw();
	// checker.draw();
	// var dummy = createGraphics(10, 10);
// }

// function draw() {
	// background(0);

// }